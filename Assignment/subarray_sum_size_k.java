import java.util.*;

public class subarray_sum_size_k {
         public static void main(String[] args)
         {
        	 Scanner sc=new Scanner(System.in);
        	 int n=sc.nextInt();
        	 int k=sc.nextInt();
        	 ArrayDeque<Integer> dq=new ArrayDeque<>();
        	 for(int i=0;i<n;i++)
        	 {
        		 dq.add(sc.nextInt());
        	 }
        	 for(int i=0;i<k;i++)
        	 {
        		 
        	 }
        	 
         }
}
